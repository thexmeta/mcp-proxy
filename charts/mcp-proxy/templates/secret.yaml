{{- if .Values.secrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mcp-proxy.fullname" . }}-secrets
  labels:
    {{- include "mcp-proxy.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}
